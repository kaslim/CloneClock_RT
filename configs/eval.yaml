# eval.yaml — 评估配置
data:
  dataset: vctk  # librispeech | vctk

eval:
  mode: eval_telephony      # eval_clean | eval_telephony | eval_defended
  max_samples: 50
  seed: 42
  defense_with_telephony: false

speaker_metric:
  encoder_name: speechbrain_ecapa
  checkpoint_dir: /home/lyx/Interspeech26/CloneBlockRT/checkpoints/speaker_encoders/speechbrain_ecapa

asr_metric:
  bundle_name: WAV2VEC2_ASR_BASE_10M
